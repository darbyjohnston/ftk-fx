Screenshots are described by screenshots.json and captured by the application
itself, one process per shot. Each shot writes a PNG and a JSON sidecar giving
the bounding box and the visible text of every widget tagged with
ftk::setScreenshotTag.

Example:
% python3 etc/Screenshots/build_screenshots.py etc/Screenshots/screenshots.json \
    --ftk-fx ../build-Debug/bin/ftk-fx/ftk-fx --out /tmp/shots

A single shot, which is the useful form while working on one:
% python3 etc/Screenshots/build_screenshots.py etc/Screenshots/screenshots.json \
    --ftk-fx ../build-Debug/bin/ftk-fx/ftk-fx --out /tmp/shots --only views-four

Setup steps a shot may use:
* {"frame": 70}                              go to a frame
* {"layout": 4}                              arrange that many panes
* {"pane": {"index": 1, "type": "Curves"}}   set what a pane shows
* {"view": {"index": 1, "type": "Top"}}      set a viewport pane's view
* {"current": 1}                             make a pane the current one
* {"lock": true}                             lock the frame at the playhead
* {"playing": true}                          start playback
* {"panel": {"name": "Diagnostics", "open": true}}   open or close a panel
* {"panelStyle": "Tabs"}                     stack the panels or tab them
* {"particleSize": 12}                       how big the particles draw
* {"draw": "Sphere"}                         discs or shaded spheres
* {"rate": 2000}                             particles emitted per second
* {"shape": {"type": "Sphere", "size": [4, 1, 4], "surface": true}}
                                             where particles are born; size and
                                             surface are optional
* {"key": {"path": "Emitter/Rate", "frame": 1, "value": 100}}
                                             key a parameter, with an optional
                                             "interp" beside it
* {"frameView": true}                        frame every viewport on the particles
* {"undo": 2}                                undo that many steps
* {"redo": 1}                                redo that many
* {"save": "/tmp/a.fx"}                      write the scene
* {"open": "/tmp/a.fx"}                      read a scene back
* {"click": [60, 80]}                        click there, with an optional
                                             "modifier" beside it
* {"drag": [[776, 619], [500, 400]]}         press there, move, release

Steps run in the order they are written. A click or a drag cannot run until
the window has been laid out, so it and everything after it are applied one
per settle; the steps before it run immediately.

Clicks go through the window the way a person's do, so they reach the parts a
setup step cannot: the click-pane-menu shot opens a pane's menu and picks an
item from it, which is the path that once crashed by rebuilding the menu from
inside its own callback. Clicks are applied after the window has been laid out,
one per settle, so a click on what a previous click opened finds it on screen.
Their coordinates are in the same pixels as the boxes in a sidecar, which is
where to read them from.

A shot may also set "settle" in seconds to wait longer before capturing, and
"splitter" in its window block to move the panel column, which is how a shot of
a wide panel gets room for it.

The type names are the labels in the pane's own menus: View, Curves,
Spreadsheet, Expression, Compositor for the content, and Persp, Front, Side, Top
for a viewport's view. An unknown one fails the shot rather than quietly
capturing the wrong thing.

DJV turns its sidecars into annotated SVGs for the documentation with a
make_svg.py step. There are no documentation pages here yet, so that step does
not exist; the "annotate" entries in the manifest are carried through to the
sidecar ready for it.

The readme shot is the one image in the repository rather than something
regenerated into a temporary directory. Remake it with:

% python3 etc/Screenshots/build_screenshots.py etc/Screenshots/screenshots.json \
    --ftk-fx ../build-Debug/bin/ftk-fx/ftk-fx --out /tmp/shots --only readme
% sips -Z 1920 /tmp/shots/readme.png --out etc/Images/ftk-fx_screenshot1.png

The capture is at the display scale, so it comes out twice the window size;
1920 across is what the other feather-tk projects use.

Coordinates in a shot are pixels, and pixels move. A shot that clicks or drags
somewhere that is no longer there does not fail -- it captures a picture of
nothing having happened, with whatever caption it was given still claiming
otherwise. panes-four-dragged did exactly that for several commits after the
panel column was widened and the splitter crossing moved out from under it.
Read the coordinates back out of a sidecar after anything moves, and check the
shot still does what it says.
